{
   "version":2,
   "builds": [
      {
         "src":"api/server.js",
         
         "config":{
            "includeFiles":["db.json"]
         }
      }
   ],
   "routes":[
      {
         "src":"/(.*)",
         "dest":"server.js"
      }
   ]
}